On extract_data.py
    - Test if the results are .json
    - if order_data_created_from < order_data_created_to
    - Consider API request retries

On get_access_token.py
    - Test if the token_file is .json
    - MAYBE if user.json is still valid on the given timespan

On get_seller_shipping_cost.py
    - Get the data from the warehouse
    - Check if the user.json is still valid
    - After making a better extraction, check if the file(s) are .json

On send_data.py